source:
  type: postgres
  url_env: RIVET_PG_URL

exports:
  - name: users_stdout_csv
    query: "SELECT id, name, email FROM users LIMIT 5"
    mode: full
    format: csv
    destination:
      type: stdout
