source:
  type: mysql
  url_env: MY_URL
  tls: { mode: require }
exports:
  - name: pa_audit
    wave: 2
    parallel_safe: true
    query: "SELECT id, name FROM pa_audit"
    mode: full
    format: parquet
    destination: { type: local, path: ./out/b07 }
