source: { type: postgres, url_env: PG_URL }
exports:
  - name: pa_audit
    table: pa_audit
    mode: incremental
    cursor_column: id
    format: parquet
    destination: { type: local, path: ./out/c05 }
