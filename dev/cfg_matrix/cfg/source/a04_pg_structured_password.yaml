source:
  type: postgres
  host: 127.0.0.1
  port: 5432
  user: rivet
  password: rivet
  database: rivet
exports:
  - name: pa_audit
    wave: 2
    parallel_safe: true
    query: "SELECT id, name FROM pa_audit"
    mode: full
    format: parquet
    destination: { type: local, path: ./out/a04 }
