source:
  type: postgres
  url_env: PG_URL
  environment: replica
exports:
  - name: pa_audit
    query: "SELECT id, name FROM pa_audit"
    mode: full
    format: parquet
    destination: { type: local, path: ./out/e18 }
