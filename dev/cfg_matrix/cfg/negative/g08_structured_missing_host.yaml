source:
  type: postgres
  user: rivet
  password: rivet
  database: rivet
exports:
  - name: pa_audit
    query: "SELECT 1"
    mode: full
    format: parquet
    destination: { type: local, path: ./out/g08 }
