source: { type: postgres, url_env: PG_URL }
exports:
  - name: pa_audit
    wave: 3
    parallel_safe: true
    table: pa_audit
    mode: chunked
    chunk_size: 10
    format: parquet
    destination: { type: local, path: ./out/g18 }
