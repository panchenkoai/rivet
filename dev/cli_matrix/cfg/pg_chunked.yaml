source:
  type: postgres
  url_env: PG_URL

exports:
  - name: pa_audit
    query: "SELECT id, name FROM pa_audit"
    mode: chunked
    chunk_column: id
    chunk_size: 10
    format: parquet
    destination:
      type: local
      path: ./out/pg_chunked
