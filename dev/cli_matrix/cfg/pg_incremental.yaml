source:
  type: postgres
  url_env: PG_URL

exports:
  - name: pa_audit
    query: "SELECT id, name FROM pa_audit"
    mode: incremental
    cursor_column: id
    format: parquet
    destination:
      type: local
      path: ./out/pg_incremental
