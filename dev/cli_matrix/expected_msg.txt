# Per-scenario stderr/stdout substring contract.
# Format:
#   <scenario_id> <stream> <+|-> <substring...>
# stream: stderr | stdout
# +  -> substring MUST appear at least once
# -  -> substring MUST NOT appear
# Lines starting with `#` and blank lines ignored.
# Quote-free: the substring runs to end-of-line (trailing spaces trimmed).
#
# Catches "rc=0 but message regressed" — invisible to expected_rc.txt.
# Add an entry whenever a CLI message is the actual user-visible contract
# (errors, hints, warnings whose absence would be a regression) or when a
# noisy log was *intentionally* removed/deduped (negative check pins it).
#
# Regenerate after intentional message changes; document in CHANGELOG.

# ── F-NEW: --param warnings (fixed in this PR) ────────────────────────────
# Used param substitutes: no "not referenced" warning, AND the resolved
# query appears in the plan JSON (proves substitution actually happened).
pg_plan_param_used   stderr - was not referenced
pg_plan_param_used   stdout + WHERE id <= 20
# Unused param: warning fires exactly once. We assert presence; check_msg.sh
# additionally enforces the count for keys flagged with `=N` suffix below.
pg_plan_param_unused stderr + --param 'unused' was not referenced
pg_plan_param_unused stderr =1 was not referenced

# ── F-NEW: plaintext-URL and TLS warnings dedup once per process ──────────
# These run scenarios used to print the password/TLS warnings 2-4× each.
pg_run_full        stderr =1 source URL contains plaintext password
pg_run_full        stderr =1 TLS is not enforced
pg_run_validate    stderr =1 source URL contains plaintext password
pg_run_validate    stderr =1 TLS is not enforced
pg_run_reconcile   stderr =1 source URL contains plaintext password
pg_run_reconcile   stderr =1 TLS is not enforced
pg_run_val_rec     stderr =1 source URL contains plaintext password
pg_run_val_rec     stderr =1 TLS is not enforced
pg_apply_drift_force stderr =1 source URL contains plaintext password
pg_apply_drift_force stderr =1 TLS is not enforced

# ── Apply: pin the gate messages so future refactors don't degrade them ────
pg_apply_drift_no_force stderr + cursor has drifted
pg_apply_drift_no_force stderr + pass --force
pg_apply_stale_no_force stderr + days old
pg_apply_stale_no_force stderr + pass --force
pg_apply_corrupt        stderr + not a valid Rivet plan artifact
pg_apply_corrupt        stderr + Hint: only files generated by `rivet plan`
pg_apply_missing        stderr + cannot read plan file
pg_apply_no_arg         stderr + the following required arguments were not provided

# ── Run: pin the actionable WARNs ─────────────────────────────────────────
pg_run_force_no_resume stderr + --force without --resume is a no-op
pg_run_resume_no_chk   stderr + --resume has no effect unless strategy is chunked
pg_run_resume_no_chk   stderr + destination prefix already has _SUCCESS

# ── Init / doctor: env-unset path ─────────────────────────────────────────
pg_init_unset    stderr + --source-env 'PG_URL' is not set in the environment
pg_doctor_unset  stdout + env var 'PG_URL' is not set
pg_doctor_unset  stdout + Some checks failed

# ── JSON-errors mode: must be valid JSON, no log preamble ─────────────────
pg_apply_json_errors stderr + {"error":
pg_apply_json_errors stderr - WARN
pg_plan_json_errors  stderr + {"error":
pg_plan_json_errors  stderr - WARN

# ── 0.7.7 audit: state reset on a typo'd export bails with hint ───────────
# Previously silently DELETE'd 0 rows + printed "State reset for export 'X'"
# as if it worked. Now lists declared exports + points at `state show`.
pg_state_reset_unknown stderr + not found in config
pg_state_reset_unknown stderr + Known exports:
pg_state_reset_unknown stderr + Hint: check the spelling
pg_state_reset_unknown stderr + rivet state show

# ── 0.7.7 audit: --type-report output shape ───────────────────────────────
# Type-report scenarios previously had rc=0 with NO content checks — a
# regression that dropped the type report entirely / silently renamed a
# fidelity tier ("exact" → "exact_match") / lost the Arrow mapping would
# stay at rc=0 unnoticed. These pin the structural skeleton.
pg_check_type_report stdout + Export: pa_audit
pg_check_type_report stdout + Source type
pg_check_type_report stdout + Rivet type
pg_check_type_report stdout + Arrow type
pg_check_type_report stdout + Fidelity
pg_check_type_report stdout + int4
pg_check_type_report stdout + Int32
pg_check_type_report stdout + text
pg_check_type_report stdout + Utf8
pg_check_type_report stdout + exact

# JSON variant: same payload, machine-readable.
pg_check_type_report_json stdout + "export":"pa_audit"
pg_check_type_report_json stdout + "source_type":"int4"
pg_check_type_report_json stdout + "arrow_type":"Int32"
pg_check_type_report_json stdout + "fidelity":"exact"
pg_check_type_report_json stdout + "violations":[]

# --target bigquery: extra Target type column appears with INT64/STRING.
pg_check_target_bq stdout + [target: bigquery]
pg_check_target_bq stdout + Target type
pg_check_target_bq stdout + INT64
pg_check_target_bq stdout + STRING
pg_check_target_bq stdout + ok
