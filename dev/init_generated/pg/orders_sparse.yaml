# Generated by rivet init — orders_sparse (~0 rows)
# Review and adjust before running: rivet check --config <this-file>

source:
  type: postgres
  url_env: DATABASE_URL  # export DATABASE_URL='<your-url>'

exports:
  - name: orders_sparse
    query: >
      SELECT id, payload
      FROM orders_sparse
    mode: full
    format: parquet
    destination:
      type: local
      path: ./dev/output
