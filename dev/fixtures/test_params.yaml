source:
  type: postgres
  url_env: RIVET_PG_URL

exports:
  - name: users_parameterized
    query: "SELECT id, name, email FROM users WHERE id <= ${MAX_ID}"
    mode: full
    format: csv
    destination:
      type: local
      path: ./dev/output
