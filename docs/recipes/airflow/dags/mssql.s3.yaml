# Generated by rivet init — SQL Server schema "dbo" (12 objects)
# Review and adjust before running: rivet check -c rivet.yaml

source:
  type: mssql
  url_env: RIVET_MS_URL
  tls: { mode: disable }

# One export per table/view — per-table prefix `exports/<name>/` under the given bucket; review
# modes before running.
exports:
  - name: bench_decimal
    wave: 3
    parallel_safe: false
    query: >
      SELECT id, price, qty, discount, total, updated_at
      FROM dbo.bench_decimal
    # auto: ~200K rows ≥ 100K threshold and chunk column 'id' is available
    mode: chunked
    chunk_column: id
    chunk_size: 100000
    chunk_checkpoint: true  # record per-chunk progress so `rivet run --resume` can finish a crashed
    # run
    format: parquet
    parquet:
      row_group_strategy: auto
      target_row_group_mb: 128
    meta_columns:  # adds 2 columns to the output (drop this block to omit them)
      exported_at: true  # _rivet_exported_at: when the row was exported
      row_hash: true  # _rivet_row_hash: per-row hash for change detection
    destination:
      type: s3
      bucket: rivet-lake
      prefix: rivet/mssql/{export}/
      region: us-east-1
      endpoint: http://host.docker.internal:9000
      allow_anonymous: true
      access_key_env: AWS_ACCESS_KEY_ID
      secret_key_env: AWS_SECRET_ACCESS_KEY
    columns:
      price: decimal(18,4)
      qty: decimal(10,2)
      discount: decimal(5,4)
      total: decimal(20,4)
  - name: bench_hc
    wave: 3
    parallel_safe: false
    query: >
      SELECT id, uuid_col, email, session_id, updated_at
      FROM dbo.bench_hc
    # auto: ~200K rows ≥ 100K threshold and chunk column 'id' is available
    mode: chunked
    chunk_column: id
    chunk_size: 100000
    chunk_checkpoint: true  # record per-chunk progress so `rivet run --resume` can finish a crashed
    # run
    format: parquet
    parquet:
      row_group_strategy: auto
      target_row_group_mb: 128
    meta_columns:  # adds 2 columns to the output (drop this block to omit them)
      exported_at: true  # _rivet_exported_at: when the row was exported
      row_hash: true  # _rivet_row_hash: per-row hash for change detection
    destination:
      type: s3
      bucket: rivet-lake
      prefix: rivet/mssql/{export}/
      region: us-east-1
      endpoint: http://host.docker.internal:9000
      allow_anonymous: true
      access_key_env: AWS_ACCESS_KEY_ID
      secret_key_env: AWS_SECRET_ACCESS_KEY
  - name: bench_narrow
    wave: 4
    parallel_safe: false
    query: >
      SELECT id, user_id, amount, status, created_at
      FROM dbo.bench_narrow
    # auto: ~10.2M rows ≥ 100K threshold and chunk column 'id' is available
    mode: chunked
    chunk_column: id
    chunk_size: 1000000
    chunk_checkpoint: true  # record per-chunk progress so `rivet run --resume` can finish a crashed
    # run
    parallel: 4
    format: parquet
    parquet:
      row_group_strategy: auto
      target_row_group_mb: 128
    meta_columns:  # adds 2 columns to the output (drop this block to omit them)
      exported_at: true  # _rivet_exported_at: when the row was exported
      row_hash: true  # _rivet_row_hash: per-row hash for change detection
    destination:
      type: s3
      bucket: rivet-lake
      prefix: rivet/mssql/{export}/
      region: us-east-1
      endpoint: http://host.docker.internal:9000
      allow_anonymous: true
      access_key_env: AWS_ACCESS_KEY_ID
      secret_key_env: AWS_SECRET_ACCESS_KEY
  - name: bench_sparse
    wave: 2
    parallel_safe: true
    query: >
      SELECT id, val_a, val_b, val_c, val_d, val_e, updated_at
      FROM dbo.bench_sparse
    # auto: ~10K rows below 100K chunked threshold
    mode: full
    format: parquet
    meta_columns:  # adds 2 columns to the output (drop this block to omit them)
      exported_at: true  # _rivet_exported_at: when the row was exported
      row_hash: true  # _rivet_row_hash: per-row hash for change detection
    destination:
      type: s3
      bucket: rivet-lake
      prefix: rivet/mssql/{export}/
      region: us-east-1
      endpoint: http://host.docker.internal:9000
      allow_anonymous: true
      access_key_env: AWS_ACCESS_KEY_ID
      secret_key_env: AWS_SECRET_ACCESS_KEY
  - name: bench_wide
    wave: 3
    parallel_safe: false
    query: >
      SELECT id, col_a, col_b, col_c, col_d, col_e, col_f, col_g, col_h, col_i, col_j, updated_at
      FROM dbo.bench_wide
    # auto: ~100K rows below 100K chunked threshold
    mode: full
    format: parquet
    meta_columns:  # adds 2 columns to the output (drop this block to omit them)
      exported_at: true  # _rivet_exported_at: when the row was exported
      row_hash: true  # _rivet_row_hash: per-row hash for change detection
    destination:
      type: s3
      bucket: rivet-lake
      prefix: rivet/mssql/{export}/
      region: us-east-1
      endpoint: http://host.docker.internal:9000
      allow_anonymous: true
      access_key_env: AWS_ACCESS_KEY_ID
      secret_key_env: AWS_SECRET_ACCESS_KEY
  - name: content_items
    wave: 3
    parallel_safe: false
    query: >
      SELECT id, title, body, raw_html, metadata, tags, author_name, author_email, source_url, category, status, priority, view_count, comment_count, word_count, language, published_at, updated_at, created_at, extra_data
      FROM dbo.content_items
    # auto: ~2.0M rows ≥ 100K threshold and chunk column 'id' is available
    mode: chunked
    chunk_column: id
    chunk_size: 250000
    chunk_checkpoint: true  # record per-chunk progress so `rivet run --resume` can finish a crashed
    # run
    parallel: 2
    format: parquet
    parquet:
      row_group_strategy: auto
      target_row_group_mb: 64
    meta_columns:  # adds 2 columns to the output (drop this block to omit them)
      exported_at: true  # _rivet_exported_at: when the row was exported
      row_hash: true  # _rivet_row_hash: per-row hash for change detection
    destination:
      type: s3
      bucket: rivet-lake
      prefix: rivet/mssql/{export}/
      region: us-east-1
      endpoint: http://host.docker.internal:9000
      allow_anonymous: true
      access_key_env: AWS_ACCESS_KEY_ID
      secret_key_env: AWS_SECRET_ACCESS_KEY
  - name: events
    wave: 2
    parallel_safe: true
    query: >
      SELECT id, user_id, event_type, payload, ip_address, created_at
      FROM dbo.events
    # auto: ~5K rows below 100K chunked threshold
    mode: full
    format: parquet
    meta_columns:  # adds 2 columns to the output (drop this block to omit them)
      exported_at: true  # _rivet_exported_at: when the row was exported
      row_hash: true  # _rivet_row_hash: per-row hash for change detection
    destination:
      type: s3
      bucket: rivet-lake
      prefix: rivet/mssql/{export}/
      region: us-east-1
      endpoint: http://host.docker.internal:9000
      allow_anonymous: true
      access_key_env: AWS_ACCESS_KEY_ID
      secret_key_env: AWS_SECRET_ACCESS_KEY
  - name: orders
    wave: 2
    parallel_safe: true
    query: >
      SELECT id, name, amount
      FROM dbo.orders
    # auto: ~500 rows below 100K chunked threshold
    mode: full
    format: parquet
    meta_columns:  # adds 2 columns to the output (drop this block to omit them)
      exported_at: true  # _rivet_exported_at: when the row was exported
      row_hash: true  # _rivet_row_hash: per-row hash for change detection
    destination:
      type: s3
      bucket: rivet-lake
      prefix: rivet/mssql/{export}/
      region: us-east-1
      endpoint: http://host.docker.internal:9000
      allow_anonymous: true
      access_key_env: AWS_ACCESS_KEY_ID
      secret_key_env: AWS_SECRET_ACCESS_KEY
    columns:
      amount: decimal(12,2)
  - name: page_views
    wave: 2
    parallel_safe: true
    query: >
      SELECT id, session_id, user_id, url, referrer, user_agent, ip_address, country_code, region, city, device_type, browser, os, screen_width, screen_height, viewport_width, viewport_height, page_load_ms, dom_ready_ms, time_on_page_ms, scroll_depth_pct, click_count, is_bounce, utm_source, utm_medium, utm_campaign, utm_term, utm_content, custom_props, created_at
      FROM dbo.page_views
    # auto: ~5K rows below 100K chunked threshold
    mode: full
    format: parquet
    meta_columns:  # adds 2 columns to the output (drop this block to omit them)
      exported_at: true  # _rivet_exported_at: when the row was exported
      row_hash: true  # _rivet_row_hash: per-row hash for change detection
    destination:
      type: s3
      bucket: rivet-lake
      prefix: rivet/mssql/{export}/
      region: us-east-1
      endpoint: http://host.docker.internal:9000
      allow_anonymous: true
      access_key_env: AWS_ACCESS_KEY_ID
      secret_key_env: AWS_SECRET_ACCESS_KEY
  - name: systranschemas
    wave: 2
    parallel_safe: true
    query: >
      SELECT tabid, startlsn, endlsn, typeid
      FROM dbo.systranschemas
    # auto: ~0 rows below 100K chunked threshold
    mode: full
    format: parquet
    meta_columns:  # adds 2 columns to the output (drop this block to omit them)
      exported_at: true  # _rivet_exported_at: when the row was exported
      row_hash: true  # _rivet_row_hash: per-row hash for change detection
    destination:
      type: s3
      bucket: rivet-lake
      prefix: rivet/mssql/{export}/
      region: us-east-1
      endpoint: http://host.docker.internal:9000
      allow_anonymous: true
      access_key_env: AWS_ACCESS_KEY_ID
      secret_key_env: AWS_SECRET_ACCESS_KEY
  - name: type_matrix
    wave: 2
    parallel_safe: true
    query: >
      SELECT id, c_bigint, c_tinyint, c_smallint, c_bit, c_float, c_real, c_decimal, c_nvarchar, c_varchar, c_uuid, c_varbinary, c_date, c_datetime2
      FROM dbo.type_matrix
    # auto: ~3 rows below 100K chunked threshold
    mode: full
    format: parquet
    meta_columns:  # adds 2 columns to the output (drop this block to omit them)
      exported_at: true  # _rivet_exported_at: when the row was exported
      row_hash: true  # _rivet_row_hash: per-row hash for change detection
    destination:
      type: s3
      bucket: rivet-lake
      prefix: rivet/mssql/{export}/
      region: us-east-1
      endpoint: http://host.docker.internal:9000
      allow_anonymous: true
      access_key_env: AWS_ACCESS_KEY_ID
      secret_key_env: AWS_SECRET_ACCESS_KEY
    columns:
      c_decimal: decimal(12,2)
  - name: users
    wave: 2
    parallel_safe: true
    query: >
      SELECT id, name, email, age, balance, is_active, bio, created_at, updated_at
      FROM dbo.users
    # auto: ~500 rows below 100K chunked threshold
    mode: full
    format: parquet
    meta_columns:  # adds 2 columns to the output (drop this block to omit them)
      exported_at: true  # _rivet_exported_at: when the row was exported
      row_hash: true  # _rivet_row_hash: per-row hash for change detection
    destination:
      type: s3
      bucket: rivet-lake
      prefix: rivet/mssql/{export}/
      region: us-east-1
      endpoint: http://host.docker.internal:9000
      allow_anonymous: true
      access_key_env: AWS_ACCESS_KEY_ID
      secret_key_env: AWS_SECRET_ACCESS_KEY
    columns:
      balance: decimal(12,2)
